class b  # comment
{
    def main() : int {
        return 10;
    }
}

class a extends b {
    var x : int;
    var x : int[];
    var x : boolean;
    var x : string;

    def x(a1: int, a2: boolean, a3: string, a4: int[]) : string {
        var x : int;
        var x : int[];
        var x : boolean;
        var x : string;
        s = new int [a + sd + -1 + 45].length;
        if(
            !x == 1 ||
            x[x*2 + 5*x] == true &&
            a+b+c+d /d/ c +d ||
            a / b * c + d - e ||
            a > b ||
            b < d
        ) then {
            writeln(hello.main((2 * (((3))) + x)));
        }

        while(a[34 + 4 + 3 + 3 ]) {
            a = a + 1;
        }

        if (i==4) then {
            i = 5;
        }
        
        return 5 + (-005);
    }
}
