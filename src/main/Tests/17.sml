class Test {
	def main() : int {
		writeln(new int[10].length);
		return 0;
	}
}

class Book {
	def main (f1: int, main: string) : int[] {
		y = a.b(); 
		z = new a().n(x, y);
		if (f.dictionary()) then
			x = (a.n()).c();
		l = ((a.n()).c()).v();
		u = ((new a()).x()).l();
		t = (A.b()).c().d();
		return a.b().c();
	}
}