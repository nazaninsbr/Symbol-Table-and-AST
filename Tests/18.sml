class getFilenames_  {
		# If this is a directory, walk each file/dir in that directory
		def main() : int{
			if (f.isDirectory()) then {
				if (a < 2) then {
					flist = f.list(32);
					while(1) {
					 	y = new s().getFilenames_(a.File(f, flist[i]), files);
					}
				}

				# // otherwise, if this is a java file, parse it!
				else		
				{
					writeln(f.getName());
					writeln(files.add(f.getAbsolutePath()));
				}
			}
			return 2424 +4 -4 * es;
		}
}