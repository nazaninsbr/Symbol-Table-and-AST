class firstClass # comment
{
    def  main( ): int{
        good = true;
        i = 0;
        data = "null";
        while( good && i < data.length ){
            good = data[i] ;
            i = i + 1;
        
        }
        return true;
    }

}
class smlExample extends exl
    {
        def main( args :string) :boolean #test*&* 
        {
            s = new Scanner();
            writeln("Enter First Number:");
            num1 = scanner.nextInt(12);
            writeln("Enter Second Number:");
            num2 = scanner.nextInt(d.test(),100);
            writeln("Enter Third Number:");
            num3 = scanner.nextInt();
            # lkakraj # Al;a;rl k# ald;alfk 1343245
            
            # In first step we are comparing only num1 and
            temp = num1 ;
            result = num3 ;
            writeln("Largest Number is:");
            return (((((23 * 5))) + d.test2()));
        }

    }

